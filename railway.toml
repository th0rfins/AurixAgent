[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

# NOTE: no healthcheck — gateway mode is a bot with outbound connections
# only (Telegram long-polling), it does not serve HTTP on $PORT.
[deploy]
startCommand = "/app/entrypoint.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
